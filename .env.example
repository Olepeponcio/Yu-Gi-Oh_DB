DB_HOST=localhost
DB_PORT=3306
DB_NAME=yugioh_db
DB_USER=tu_usuario
DB_PASSWORD=tu_password
# Solo si la base no es local y se acepta explicitamente el riesgo:
# DB_ALLOW_REMOTE_DB=false
# Limite maximo del JSON raw descargado desde la API, en bytes:
# YGOPRODECK_MAX_RAW_BYTES=78643200
